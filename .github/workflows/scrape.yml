name: scrape-dgbas

on:
  schedule:
    - cron: "15 2 * * *" # 每日 02:15 UTC（台灣 10:15）
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm install --no-audit --no-fund
      - run: npm run scrape

      - uses: actions/upload-pages-artifact@v3
        with:
          # 發布 public 資料夾 → 站台根目錄即含 index.html 與 median.json
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages

    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
